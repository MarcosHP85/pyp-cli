const askIfsCredentials = require('./credentials')
const askTableColumns = require('./columns')
const askIfsTable = require('./table')
const askFolder = require('./folder')

module.exports = {
  askIfsCredentials,
  askTableColumns,
  askIfsTable,
  askFolder
}
